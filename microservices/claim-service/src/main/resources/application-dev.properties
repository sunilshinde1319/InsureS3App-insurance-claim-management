# Local-only configuration for claim-service (DO NOT COMMIT)

# --- Database ---
spring.datasource.url=jdbc:mysql://localhost:3306/claim_service_db
spring.datasource.username=root
spring.datasource.password=Sunil@1319

# --- JWT Secret ---
jwt.secret=thisisalongandsecuresecretkeyformyinsuranceclaimsystem

# --- Service URLs for Localhost ---
user.service.url=http://localhost:8081
notification.service.url=http://localhost:8084
payment.service.url=http://localhost:8086

# --- Hibernate / JPA Config ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
